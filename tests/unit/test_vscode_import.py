from auto_questionnaire.ai.groq_handler import GroqHandler


def test_groq_handler():
    # 在这里输入 Groq，然后等一下或按 Ctrl + Space
    # 你会看到提示框，选择 GroqHandler
    handler = GroqHandler()  # 此时类名会显示红色下划线
    
    # 2. 将鼠标悬停在 GroqHandler 上
    # 会看到一个灯泡图标 💡 或按 Ctrl + .
    # 点击灯泡或按 Ctrl + .，选择 "Import GroqHandler"
